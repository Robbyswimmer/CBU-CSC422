# CSCI 4220 — everything the coding assignments import.
#
# Google Colab already has all of this; install locally only if you are
# running the notebooks on your own machine:
#
#     python3 -m venv venv && source venv/bin/activate
#     pip install -r requirements.txt
#
# On an Apple Silicon Mac the default torch wheel includes the 'mps' GPU
# backend, which CA.04 and CA.05 use automatically.

numpy
matplotlib
scikit-learn
torch          # CA.03, CA.04, CA.05
torchvision    # CA.04
pillow         # CA.04
tqdm           # CA.04, CA.05
pandas         # CA.02
seaborn        # optional — plot styling only; every notebook runs without it
